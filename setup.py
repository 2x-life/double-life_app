from setuptools import setup, find_packages

setup(
    name="doublelife_gamebook",
    packages=find_packages(),
    version=0.1,
    install_requires=[],
    author="Adrien Gougeon",
    # ~ entry_points={
    # ~ 'console_scripts': [
        # ~ 'doublelife_gamebook = doublelife_gamebook.main:main',
        # ~ ]
    # ~ },
)
